# Automatic-File-Sorter
Automatic File Sorter is a Python automation tool that organizes files based on their extensions. It scans a selected directory, creates folders for different file types, and moves files automatically. Built with os, shutil, and pathlib, it simplifies file management and keeps directories clean and organized.
